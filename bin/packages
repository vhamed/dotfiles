#!/usr/bin/env bash

# This file contains list of packages I used
# uncomment package to install it

#---- required (at least for me)
sudo apt -y install curl 
sudo apt -y install git 
sudo apt -y install rofi

#---- snap (package management system from canonical)
# sudo apt -y install snapd

#---- pip3 
# sudo apt -y install python3-pip

# sudo apt -y install konsole 
sudo apt -y install kitty 
sudo apt -y install cmus
sudo apt -y install ranger
sudo apt -y install htop
# sudo apt -y install uget

#---- utilities
sudo apt -y install keepassx
# sudo apt -y install vlc
# sudo apt -y install chromium-browser
# sudo apt install youtube-dl
# sudo snap install mailspring
# sudo snap install discord

#---- dev tools ----
# pip3 install litecli
# sudo apt -y install mycli
# sudo snap install postman

#---- npm && node
# curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash -
# sudo apt -y update
# sudo apt -y install nodejs

#---- yarn
# curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
# echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
# sudo apt -y update
# sudo apt install yarn

#---- global npm modules 
# sudo npm install -g neovim
# npm install -g create-react-app
# npm install -g gatsby-cli

#---- brave-browser
# curl -s https://brave-browser-apt-release.s3.brave.com/brave-core.asc | sudo apt-key --keyring /etc/apt/trusted.gpg.d/brave-browser-release.gpg add -
# echo "deb [arch=amd64] https://brave-browser-apt-release.s3.brave.com/ stable main" | sudo tee /etc/apt/sources.list.d/brave-browser-release.list
# sudo apt -y update
# sudo apt install brave-browser

#---- google-chrome
# wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
# sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
# sudo apt update
# sudo apt install google-chrome-stable

#---- php
# sudo apt -y install software-properties-common
# sudo add-apt-repository ppa:ondrej/php
# sudo apt -y install php 
# sudo apt -y install php7.x-{common,curl,json,mbstring,mysql,xml,zip}
# sudo apt -y install nginx 
# sudo apt -y install php7.x-fpm
# sudo apt -y install lamp-server^

#---- composer
# curl -sS https://getcomposer.org/installer | php
# sudo mv composer.phar /usr/local/bin/composer
# chmod +x /usr/local/bin/composer

#---- laravel
# composer global require laravel/installer
